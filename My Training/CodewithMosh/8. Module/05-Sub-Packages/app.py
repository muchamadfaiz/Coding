# Sub-Packages
# as yout program grow you may want to break a package down into sub-packages
# so lets add a new file called shopping a sub directory of ecommerce package
# after that to make ashopping directory as a package we make a file python called __init__.py

from ecommerce.shopping import sales
